export enum CharactersActionType {
  Load = "LOAD",
  Success = "SUCCESS",
  Error = "Error",
}

export enum CharacterActionType {
  Load = "LOAD",
  Success = "SUCCESS",
  Error = "ERROR",
  Reset = "RESET",
}
